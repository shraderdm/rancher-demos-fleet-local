apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: monitoring
spec:
  branch: main
  insecureSkipTLSVerify: false
  paths:
  - gatekeeper
  repo: https://github.com/nikkelma/rancher-demos-fleet-default
  targets:
  - clusterGroup: gatekeeper-enabled
